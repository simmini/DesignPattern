*빌더패턴
복잡한 단계를 거쳐야 생성되는 객체의 구현을 서브클래스에게 넘겨주는 패턴

학습목표
복잡한 단계가 필요한 인스턴스 생성을 빌더 패턴을 통해서 구현할 수 있다.

알려진 빌더 패턴 두가지
1. 생성시 지정해야 할 인자가 많을 때  사용하는 패턴
2. 객체 생성시 여러 단계를 순차적으로 거칠때,
   이 단계의 순서를 결정해 두고 각 단계를 다양하게 구현할수 있도록 하는 패턴
   
   
  ->주로 1번 패턴으로 사용되지만 빌더패턴에 대한 설명은 2번으로 주로 설명되고있다.